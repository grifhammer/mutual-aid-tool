# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

version: "3.7"
services:
  backend:
    build:
      context: backend
      target: development
    depends_on:
      - db
    container_name: hapijs
    environment:
      MONGO_DB_URI: mongodb://root:xky6KBy3YHK8sN6TGH5g@db:27017/ma-tool
      NODE_ENV: development
  db:
    image: mongo
    restart: always
    expose:
      - 27017
    container_name: mongo-database
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: xky6KBy3YHK8sN6TGH5g
  frontend:
    build:
      context: web
      target: development
    depends_on:
      - backend
    expose:
      - 3000
    container_name: nextjs
