# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

version: "3.7"
services:
  backend:
    build:
      context: backend
      target: development
    dependsOn: db
  db:
    image: mongo
    restart: always
    environment: {}
    expose:
      - 27017
  frontend: {}
